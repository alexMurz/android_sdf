@file:Suppress("unused", "SpellCheckingInspection")

package com.example.spheremarch.math

//////////////////
// X-

// *X--
val Vec4.xxxx: Vec4; inline get() = vec4(x, x, x, x)
val Vec4.xxxy: Vec4; inline get() = vec4(x, x, x, y)
val Vec4.xxxz: Vec4; inline get() = vec4(x, x, x, z)
val Vec4.xxxw: Vec4; inline get() = vec4(x, x, x, w)

val Vec4.xxyx: Vec4; inline get() = vec4(x, x, y, x)
val Vec4.xxyy: Vec4; inline get() = vec4(x, x, y, y)
val Vec4.xxyz: Vec4; inline get() = vec4(x, x, y, z)
val Vec4.xxyw: Vec4; inline get() = vec4(x, x, y, w)

val Vec4.xxzx: Vec4; inline get() = vec4(x, x, z, x)
val Vec4.xxzy: Vec4; inline get() = vec4(x, x, z, y)
val Vec4.xxzz: Vec4; inline get() = vec4(x, x, z, z)
val Vec4.xxzw: Vec4; inline get() = vec4(x, x, z, w)

val Vec4.xxwx: Vec4; inline get() = vec4(x, x, w, x)
val Vec4.xxwy: Vec4; inline get() = vec4(x, x, w, y)
val Vec4.xxwz: Vec4; inline get() = vec4(x, x, w, z)
val Vec4.xxww: Vec4; inline get() = vec4(x, x, w, w)

// *Y--
val Vec4.xyxx: Vec4; inline get() = vec4(x, y, x, x)
val Vec4.xyxy: Vec4; inline get() = vec4(x, y, x, y)
val Vec4.xyxz: Vec4; inline get() = vec4(x, y, x, z)
val Vec4.xyxw: Vec4; inline get() = vec4(x, y, x, w)

val Vec4.xyyx: Vec4; inline get() = vec4(x, y, y, x)
val Vec4.xyyy: Vec4; inline get() = vec4(x, y, y, y)
val Vec4.xyyz: Vec4; inline get() = vec4(x, y, y, z)
val Vec4.xyyw: Vec4; inline get() = vec4(x, y, y, w)

val Vec4.xyzx: Vec4; inline get() = vec4(x, y, z, x)
val Vec4.xyzy: Vec4; inline get() = vec4(x, y, z, y)
val Vec4.xyzz: Vec4; inline get() = vec4(x, y, z, z)
val Vec4.xyzw: Vec4; inline get() = vec4(x, y, z, w)

val Vec4.xywx: Vec4; inline get() = vec4(x, y, w, x)
val Vec4.xywy: Vec4; inline get() = vec4(x, y, w, y)
val Vec4.xywz: Vec4; inline get() = vec4(x, y, w, z)
val Vec4.xyww: Vec4; inline get() = vec4(x, y, w, w)

// *Z--
val Vec4.xzxx: Vec4; inline get() = vec4(x, z, x, x)
val Vec4.xzxy: Vec4; inline get() = vec4(x, z, x, y)
val Vec4.xzxz: Vec4; inline get() = vec4(x, z, x, z)
val Vec4.xzxw: Vec4; inline get() = vec4(x, z, x, w)

val Vec4.xzyx: Vec4; inline get() = vec4(x, z, y, x)
val Vec4.xzyy: Vec4; inline get() = vec4(x, z, y, y)
val Vec4.xzyz: Vec4; inline get() = vec4(x, z, y, z)
val Vec4.xzyw: Vec4; inline get() = vec4(x, z, y, w)

val Vec4.xzzx: Vec4; inline get() = vec4(x, z, z, x)
val Vec4.xzzy: Vec4; inline get() = vec4(x, z, z, y)
val Vec4.xzzz: Vec4; inline get() = vec4(x, z, z, z)
val Vec4.xzzw: Vec4; inline get() = vec4(x, z, z, w)

val Vec4.xzwx: Vec4; inline get() = vec4(x, z, w, x)
val Vec4.xzwy: Vec4; inline get() = vec4(x, z, w, y)
val Vec4.xzwz: Vec4; inline get() = vec4(x, z, w, z)
val Vec4.xzww: Vec4; inline get() = vec4(x, z, w, w)

// *W--
val Vec4.xwxx: Vec4; inline get() = vec4(x, w, x, x)
val Vec4.xwxy: Vec4; inline get() = vec4(x, w, x, y)
val Vec4.xwxz: Vec4; inline get() = vec4(x, w, x, z)
val Vec4.xwxw: Vec4; inline get() = vec4(x, w, x, w)

val Vec4.xwyx: Vec4; inline get() = vec4(x, w, y, x)
val Vec4.xwyy: Vec4; inline get() = vec4(x, w, y, y)
val Vec4.xwyz: Vec4; inline get() = vec4(x, w, y, z)
val Vec4.xwyw: Vec4; inline get() = vec4(x, w, y, w)

val Vec4.xwzx: Vec4; inline get() = vec4(x, w, z, x)
val Vec4.xwzy: Vec4; inline get() = vec4(x, w, z, y)
val Vec4.xwzz: Vec4; inline get() = vec4(x, w, z, z)
val Vec4.xwzw: Vec4; inline get() = vec4(x, w, z, w)

val Vec4.xwwx: Vec4; inline get() = vec4(x, w, w, x)
val Vec4.xwwy: Vec4; inline get() = vec4(x, w, w, y)
val Vec4.xwwz: Vec4; inline get() = vec4(x, w, w, z)
val Vec4.xwww: Vec4; inline get() = vec4(x, w, w, w)

//////////////////
// Y-

// *X--
val Vec4.yxxx: Vec4; inline get() = vec4(y, x, x, x)
val Vec4.yxxy: Vec4; inline get() = vec4(y, x, x, y)
val Vec4.yxxz: Vec4; inline get() = vec4(y, x, x, z)
val Vec4.yxxw: Vec4; inline get() = vec4(y, x, x, w)

val Vec4.yxyx: Vec4; inline get() = vec4(y, x, y, x)
val Vec4.yxyy: Vec4; inline get() = vec4(y, x, y, y)
val Vec4.yxyz: Vec4; inline get() = vec4(y, x, y, z)
val Vec4.yxyw: Vec4; inline get() = vec4(y, x, y, w)

val Vec4.yxzx: Vec4; inline get() = vec4(y, x, z, x)
val Vec4.yxzy: Vec4; inline get() = vec4(y, x, z, y)
val Vec4.yxzz: Vec4; inline get() = vec4(y, x, z, z)
val Vec4.yxzw: Vec4; inline get() = vec4(y, x, z, w)

val Vec4.yxwx: Vec4; inline get() = vec4(y, x, w, x)
val Vec4.yxwy: Vec4; inline get() = vec4(y, x, w, y)
val Vec4.yxwz: Vec4; inline get() = vec4(y, x, w, z)
val Vec4.yxww: Vec4; inline get() = vec4(y, x, w, w)

// *Y--
val Vec4.yyxx: Vec4; inline get() = vec4(y, y, x, x)
val Vec4.yyxy: Vec4; inline get() = vec4(y, y, x, y)
val Vec4.yyxz: Vec4; inline get() = vec4(y, y, x, z)
val Vec4.yyxw: Vec4; inline get() = vec4(y, y, x, w)

val Vec4.yyyx: Vec4; inline get() = vec4(y, y, y, x)
val Vec4.yyyy: Vec4; inline get() = vec4(y, y, y, y)
val Vec4.yyyz: Vec4; inline get() = vec4(y, y, y, z)
val Vec4.yyyw: Vec4; inline get() = vec4(y, y, y, w)

val Vec4.yyzx: Vec4; inline get() = vec4(y, y, z, x)
val Vec4.yyzy: Vec4; inline get() = vec4(y, y, z, y)
val Vec4.yyzz: Vec4; inline get() = vec4(y, y, z, z)
val Vec4.yyzw: Vec4; inline get() = vec4(y, y, z, w)

val Vec4.yywx: Vec4; inline get() = vec4(y, y, w, x)
val Vec4.yywy: Vec4; inline get() = vec4(y, y, w, y)
val Vec4.yywz: Vec4; inline get() = vec4(y, y, w, z)
val Vec4.yyww: Vec4; inline get() = vec4(y, y, w, w)

// *Z--
val Vec4.yzxx: Vec4; inline get() = vec4(y, z, x, x)
val Vec4.yzxy: Vec4; inline get() = vec4(y, z, x, y)
val Vec4.yzxz: Vec4; inline get() = vec4(y, z, x, z)
val Vec4.yzxw: Vec4; inline get() = vec4(y, z, x, w)

val Vec4.yzyx: Vec4; inline get() = vec4(y, z, y, x)
val Vec4.yzyy: Vec4; inline get() = vec4(y, z, y, y)
val Vec4.yzyz: Vec4; inline get() = vec4(y, z, y, z)
val Vec4.yzyw: Vec4; inline get() = vec4(y, z, y, w)

val Vec4.yzzx: Vec4; inline get() = vec4(y, z, z, x)
val Vec4.yzzy: Vec4; inline get() = vec4(y, z, z, y)
val Vec4.yzzz: Vec4; inline get() = vec4(y, z, z, z)
val Vec4.yzzw: Vec4; inline get() = vec4(y, z, z, w)

val Vec4.yzwx: Vec4; inline get() = vec4(y, z, w, x)
val Vec4.yzwy: Vec4; inline get() = vec4(y, z, w, y)
val Vec4.yzwz: Vec4; inline get() = vec4(y, z, w, z)
val Vec4.yzww: Vec4; inline get() = vec4(y, z, w, w)

// *Z--
val Vec4.ywxx: Vec4; inline get() = vec4(y, w, x, x)
val Vec4.ywxy: Vec4; inline get() = vec4(y, w, x, y)
val Vec4.ywxz: Vec4; inline get() = vec4(y, w, x, z)
val Vec4.ywxw: Vec4; inline get() = vec4(y, w, x, w)

val Vec4.ywyx: Vec4; inline get() = vec4(y, w, y, x)
val Vec4.ywyy: Vec4; inline get() = vec4(y, w, y, y)
val Vec4.ywyz: Vec4; inline get() = vec4(y, w, y, z)
val Vec4.ywyw: Vec4; inline get() = vec4(y, w, y, w)

val Vec4.ywzx: Vec4; inline get() = vec4(y, w, z, x)
val Vec4.ywzy: Vec4; inline get() = vec4(y, w, z, y)
val Vec4.ywzz: Vec4; inline get() = vec4(y, w, z, z)
val Vec4.ywzw: Vec4; inline get() = vec4(y, w, z, w)

val Vec4.ywwx: Vec4; inline get() = vec4(y, w, w, x)
val Vec4.ywwy: Vec4; inline get() = vec4(y, w, w, y)
val Vec4.ywwz: Vec4; inline get() = vec4(y, w, w, z)
val Vec4.ywww: Vec4; inline get() = vec4(y, w, w, w)


//////////////////
// Z-

// *X--
val Vec4.zxxx: Vec4; inline get() = vec4(z, x, x, x)
val Vec4.zxxy: Vec4; inline get() = vec4(z, x, x, y)
val Vec4.zxxz: Vec4; inline get() = vec4(z, x, x, z)
val Vec4.zxxw: Vec4; inline get() = vec4(z, x, x, w)

val Vec4.zxyx: Vec4; inline get() = vec4(z, x, y, x)
val Vec4.zxyy: Vec4; inline get() = vec4(z, x, y, y)
val Vec4.zxyz: Vec4; inline get() = vec4(z, x, y, z)
val Vec4.zxyw: Vec4; inline get() = vec4(z, x, y, w)

val Vec4.zxzx: Vec4; inline get() = vec4(z, x, z, x)
val Vec4.zxzy: Vec4; inline get() = vec4(z, x, z, y)
val Vec4.zxzz: Vec4; inline get() = vec4(z, x, z, z)
val Vec4.zxzw: Vec4; inline get() = vec4(z, x, z, w)

val Vec4.zxwx: Vec4; inline get() = vec4(z, x, w, x)
val Vec4.zxwy: Vec4; inline get() = vec4(z, x, w, y)
val Vec4.zxwz: Vec4; inline get() = vec4(z, x, w, z)
val Vec4.zxww: Vec4; inline get() = vec4(z, x, w, w)

// *Y--
val Vec4.zyxx: Vec4; inline get() = vec4(z, y, x, x)
val Vec4.zyxy: Vec4; inline get() = vec4(z, y, x, y)
val Vec4.zyxz: Vec4; inline get() = vec4(z, y, x, z)
val Vec4.zyxw: Vec4; inline get() = vec4(z, y, x, w)

val Vec4.zyyx: Vec4; inline get() = vec4(z, y, y, x)
val Vec4.zyyy: Vec4; inline get() = vec4(z, y, y, y)
val Vec4.zyyz: Vec4; inline get() = vec4(z, y, y, z)
val Vec4.zyyw: Vec4; inline get() = vec4(z, y, y, w)

val Vec4.zyzx: Vec4; inline get() = vec4(z, y, z, x)
val Vec4.zyzy: Vec4; inline get() = vec4(z, y, z, y)
val Vec4.zyzz: Vec4; inline get() = vec4(z, y, z, z)
val Vec4.zyzw: Vec4; inline get() = vec4(z, y, z, w)

val Vec4.zywx: Vec4; inline get() = vec4(z, y, w, x)
val Vec4.zywy: Vec4; inline get() = vec4(z, y, w, y)
val Vec4.zywz: Vec4; inline get() = vec4(z, y, w, z)
val Vec4.zyww: Vec4; inline get() = vec4(z, y, w, w)

// *Z--
val Vec4.zzxx: Vec4; inline get() = vec4(z, z, x, x)
val Vec4.zzxy: Vec4; inline get() = vec4(z, z, x, y)
val Vec4.zzxz: Vec4; inline get() = vec4(z, z, x, z)
val Vec4.zzxw: Vec4; inline get() = vec4(z, z, x, w)

val Vec4.zzyx: Vec4; inline get() = vec4(z, z, y, x)
val Vec4.zzyy: Vec4; inline get() = vec4(z, z, y, y)
val Vec4.zzyz: Vec4; inline get() = vec4(z, z, y, z)
val Vec4.zzyw: Vec4; inline get() = vec4(z, z, y, w)

val Vec4.zzzx: Vec4; inline get() = vec4(z, z, z, x)
val Vec4.zzzy: Vec4; inline get() = vec4(z, z, z, y)
val Vec4.zzzz: Vec4; inline get() = vec4(z, z, z, z)
val Vec4.zzzw: Vec4; inline get() = vec4(z, z, z, w)

val Vec4.zzwx: Vec4; inline get() = vec4(z, z, w, x)
val Vec4.zzwy: Vec4; inline get() = vec4(z, z, w, y)
val Vec4.zzwz: Vec4; inline get() = vec4(z, z, w, z)
val Vec4.zzww: Vec4; inline get() = vec4(z, z, w, w)

// *Z--
val Vec4.zwxx: Vec4; inline get() = vec4(z, w, x, x)
val Vec4.zwxy: Vec4; inline get() = vec4(z, w, x, y)
val Vec4.zwxz: Vec4; inline get() = vec4(z, w, x, z)
val Vec4.zwxw: Vec4; inline get() = vec4(z, w, x, w)

val Vec4.zwyx: Vec4; inline get() = vec4(z, w, y, x)
val Vec4.zwyy: Vec4; inline get() = vec4(z, w, y, y)
val Vec4.zwyz: Vec4; inline get() = vec4(z, w, y, z)
val Vec4.zwyw: Vec4; inline get() = vec4(z, w, y, w)

val Vec4.zwzx: Vec4; inline get() = vec4(z, w, z, x)
val Vec4.zwzy: Vec4; inline get() = vec4(z, w, z, y)
val Vec4.zwzz: Vec4; inline get() = vec4(z, w, z, z)
val Vec4.zwzw: Vec4; inline get() = vec4(z, w, z, w)

val Vec4.zwwx: Vec4; inline get() = vec4(z, w, w, x)
val Vec4.zwwy: Vec4; inline get() = vec4(z, w, w, y)
val Vec4.zwwz: Vec4; inline get() = vec4(z, w, w, z)
val Vec4.zwww: Vec4; inline get() = vec4(z, w, w, w)


//////////////////
// W-

// *X--
val Vec4.wxxx: Vec4; inline get() = vec4(w, x, x, x)
val Vec4.wxxy: Vec4; inline get() = vec4(w, x, x, y)
val Vec4.wxxz: Vec4; inline get() = vec4(w, x, x, z)
val Vec4.wxxw: Vec4; inline get() = vec4(w, x, x, w)

val Vec4.wxyx: Vec4; inline get() = vec4(w, x, y, x)
val Vec4.wxyy: Vec4; inline get() = vec4(w, x, y, y)
val Vec4.wxyz: Vec4; inline get() = vec4(w, x, y, z)
val Vec4.wxyw: Vec4; inline get() = vec4(w, x, y, w)

val Vec4.wxzx: Vec4; inline get() = vec4(w, x, z, x)
val Vec4.wxzy: Vec4; inline get() = vec4(w, x, z, y)
val Vec4.wxzz: Vec4; inline get() = vec4(w, x, z, z)
val Vec4.wxzw: Vec4; inline get() = vec4(w, x, z, w)

val Vec4.wxwx: Vec4; inline get() = vec4(w, x, w, x)
val Vec4.wxwy: Vec4; inline get() = vec4(w, x, w, y)
val Vec4.wxwz: Vec4; inline get() = vec4(w, x, w, z)
val Vec4.wxww: Vec4; inline get() = vec4(w, x, w, w)

// *Y--
val Vec4.wyxx: Vec4; inline get() = vec4(w, y, x, x)
val Vec4.wyxy: Vec4; inline get() = vec4(w, y, x, y)
val Vec4.wyxz: Vec4; inline get() = vec4(w, y, x, z)
val Vec4.wyxw: Vec4; inline get() = vec4(w, y, x, w)

val Vec4.wyyx: Vec4; inline get() = vec4(w, y, y, x)
val Vec4.wyyy: Vec4; inline get() = vec4(w, y, y, y)
val Vec4.wyyz: Vec4; inline get() = vec4(w, y, y, z)
val Vec4.wyyw: Vec4; inline get() = vec4(w, y, y, w)

val Vec4.wyzx: Vec4; inline get() = vec4(w, y, z, x)
val Vec4.wyzy: Vec4; inline get() = vec4(w, y, z, y)
val Vec4.wyzz: Vec4; inline get() = vec4(w, y, z, z)
val Vec4.wyzw: Vec4; inline get() = vec4(w, y, z, w)

val Vec4.wywx: Vec4; inline get() = vec4(w, y, w, x)
val Vec4.wywy: Vec4; inline get() = vec4(w, y, w, y)
val Vec4.wywz: Vec4; inline get() = vec4(w, y, w, z)
val Vec4.wyww: Vec4; inline get() = vec4(w, y, w, w)

// *Z--
val Vec4.wzxx: Vec4; inline get() = vec4(w, z, x, x)
val Vec4.wzxy: Vec4; inline get() = vec4(w, z, x, y)
val Vec4.wzxz: Vec4; inline get() = vec4(w, z, x, z)
val Vec4.wzxw: Vec4; inline get() = vec4(w, z, x, w)

val Vec4.wzyx: Vec4; inline get() = vec4(w, z, y, x)
val Vec4.wzyy: Vec4; inline get() = vec4(w, z, y, y)
val Vec4.wzyz: Vec4; inline get() = vec4(w, z, y, z)
val Vec4.wzyw: Vec4; inline get() = vec4(w, z, y, w)

val Vec4.wzzx: Vec4; inline get() = vec4(w, z, z, x)
val Vec4.wzzy: Vec4; inline get() = vec4(w, z, z, y)
val Vec4.wzzz: Vec4; inline get() = vec4(w, z, z, z)
val Vec4.wzzw: Vec4; inline get() = vec4(w, z, z, w)

val Vec4.wzwx: Vec4; inline get() = vec4(w, z, w, x)
val Vec4.wzwy: Vec4; inline get() = vec4(w, z, w, y)
val Vec4.wzwz: Vec4; inline get() = vec4(w, z, w, z)
val Vec4.wzww: Vec4; inline get() = vec4(w, z, w, w)

// *Z--
val Vec4.wwxx: Vec4; inline get() = vec4(w, w, x, x)
val Vec4.wwxy: Vec4; inline get() = vec4(w, w, x, y)
val Vec4.wwxz: Vec4; inline get() = vec4(w, w, x, z)
val Vec4.wwxw: Vec4; inline get() = vec4(w, w, x, w)

val Vec4.wwyx: Vec4; inline get() = vec4(w, w, y, x)
val Vec4.wwyy: Vec4; inline get() = vec4(w, w, y, y)
val Vec4.wwyz: Vec4; inline get() = vec4(w, w, y, z)
val Vec4.wwyw: Vec4; inline get() = vec4(w, w, y, w)

val Vec4.wwzx: Vec4; inline get() = vec4(w, w, z, x)
val Vec4.wwzy: Vec4; inline get() = vec4(w, w, z, y)
val Vec4.wwzz: Vec4; inline get() = vec4(w, w, z, z)
val Vec4.wwzw: Vec4; inline get() = vec4(w, w, z, w)

val Vec4.wwwx: Vec4; inline get() = vec4(w, w, w, x)
val Vec4.wwwy: Vec4; inline get() = vec4(w, w, w, y)
val Vec4.wwwz: Vec4; inline get() = vec4(w, w, w, z)
val Vec4.wwww: Vec4; inline get() = vec4(w, w, w, w)


//////////////////
// Vec3

// X--
val Vec4.xxx: Vec3; inline get() = vec3(x, x, x)
val Vec4.xxy: Vec3; inline get() = vec3(x, x, y)
val Vec4.xxz: Vec3; inline get() = vec3(x, x, z)
val Vec4.xxw: Vec3; inline get() = vec3(x, x, w)

val Vec4.xyx: Vec3; inline get() = vec3(x, y, x)
val Vec4.xyy: Vec3; inline get() = vec3(x, y, y)
val Vec4.xyz: Vec3; inline get() = vec3(x, y, z)
val Vec4.xyw: Vec3; inline get() = vec3(x, y, w)

val Vec4.xzx: Vec3; inline get() = vec3(x, z, x)
val Vec4.xzy: Vec3; inline get() = vec3(x, z, y)
val Vec4.xzz: Vec3; inline get() = vec3(x, z, z)
val Vec4.xzw: Vec3; inline get() = vec3(x, z, w)

val Vec4.xwx: Vec3; inline get() = vec3(x, w, x)
val Vec4.xwy: Vec3; inline get() = vec3(x, w, y)
val Vec4.xwz: Vec3; inline get() = vec3(x, w, z)
val Vec4.xww: Vec3; inline get() = vec3(x, w, w)

// Y--
val Vec4.yxx: Vec3; inline get() = vec3(y, x, x)
val Vec4.yxy: Vec3; inline get() = vec3(y, x, y)
val Vec4.yxz: Vec3; inline get() = vec3(y, x, z)
val Vec4.yxw: Vec3; inline get() = vec3(y, x, w)

val Vec4.yyx: Vec3; inline get() = vec3(y, y, x)
val Vec4.yyy: Vec3; inline get() = vec3(y, y, y)
val Vec4.yyz: Vec3; inline get() = vec3(y, y, z)
val Vec4.yyw: Vec3; inline get() = vec3(y, y, w)

val Vec4.yzx: Vec3; inline get() = vec3(y, z, x)
val Vec4.yzy: Vec3; inline get() = vec3(y, z, y)
val Vec4.yzz: Vec3; inline get() = vec3(y, z, z)
val Vec4.yzw: Vec3; inline get() = vec3(y, z, w)

val Vec4.ywx: Vec3; inline get() = vec3(y, w, x)
val Vec4.ywy: Vec3; inline get() = vec3(y, w, y)
val Vec4.ywz: Vec3; inline get() = vec3(y, w, z)
val Vec4.yww: Vec3; inline get() = vec3(y, w, w)

// Z--
val Vec4.zxx: Vec3; inline get() = vec3(z, x, x)
val Vec4.zxy: Vec3; inline get() = vec3(z, x, y)
val Vec4.zxz: Vec3; inline get() = vec3(z, x, z)
val Vec4.zxw: Vec3; inline get() = vec3(z, x, w)

val Vec4.zyx: Vec3; inline get() = vec3(z, y, x)
val Vec4.zyy: Vec3; inline get() = vec3(z, y, y)
val Vec4.zyz: Vec3; inline get() = vec3(z, y, z)
val Vec4.zyw: Vec3; inline get() = vec3(z, y, w)

val Vec4.zzx: Vec3; inline get() = vec3(z, z, x)
val Vec4.zzy: Vec3; inline get() = vec3(z, z, y)
val Vec4.zzz: Vec3; inline get() = vec3(z, z, z)
val Vec4.zzw: Vec3; inline get() = vec3(z, z, w)

val Vec4.zwx: Vec3; inline get() = vec3(z, w, x)
val Vec4.zwy: Vec3; inline get() = vec3(z, w, y)
val Vec4.zwz: Vec3; inline get() = vec3(z, w, z)
val Vec4.zww: Vec3; inline get() = vec3(z, w, w)

// W--
val Vec4.wxx: Vec3; inline get() = vec3(w, x, x)
val Vec4.wxy: Vec3; inline get() = vec3(w, x, y)
val Vec4.wxz: Vec3; inline get() = vec3(w, x, z)
val Vec4.wxw: Vec3; inline get() = vec3(w, x, w)

val Vec4.wyx: Vec3; inline get() = vec3(w, y, x)
val Vec4.wyy: Vec3; inline get() = vec3(w, y, y)
val Vec4.wyz: Vec3; inline get() = vec3(w, y, z)
val Vec4.wyw: Vec3; inline get() = vec3(w, y, w)

val Vec4.wzx: Vec3; inline get() = vec3(w, z, x)
val Vec4.wzy: Vec3; inline get() = vec3(w, z, y)
val Vec4.wzz: Vec3; inline get() = vec3(w, z, z)
val Vec4.wzw: Vec3; inline get() = vec3(w, z, w)

val Vec4.wwx: Vec3; inline get() = vec3(w, w, x)
val Vec4.wwy: Vec3; inline get() = vec3(w, w, y)
val Vec4.wwz: Vec3; inline get() = vec3(w, w, z)
val Vec4.www: Vec3; inline get() = vec3(w, w, w)

// Vec2
// *Z--
val Vec4.xx: Vec2; inline get() = vec2(x, x)
val Vec4.xy: Vec2; inline get() = vec2(x, y)
val Vec4.xz: Vec2; inline get() = vec2(x, z)
val Vec4.xw: Vec2; inline get() = vec2(x, w)

val Vec4.yx: Vec2; inline get() = vec2(y, x)
val Vec4.yy: Vec2; inline get() = vec2(y, y)
val Vec4.yz: Vec2; inline get() = vec2(y, z)
val Vec4.yw: Vec2; inline get() = vec2(y, w)

val Vec4.zx: Vec2; inline get() = vec2(z, x)
val Vec4.zy: Vec2; inline get() = vec2(z, y)
val Vec4.zz: Vec2; inline get() = vec2(z, z)
val Vec4.zw: Vec2; inline get() = vec2(z, w)

val Vec4.wx: Vec2; inline get() = vec2(w, x)
val Vec4.wy: Vec2; inline get() = vec2(w, y)
val Vec4.wz: Vec2; inline get() = vec2(w, z)
val Vec4.ww: Vec2; inline get() = vec2(w, w)

